#include <stdio.h>
#include <stdlib.h>

int MAX=5;
int top=-1;   //top

// function to check if satck is empty
int isEmpty(){
    if(top== -1){
        return 1;
    }
    else 
    return 0;
}